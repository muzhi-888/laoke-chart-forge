---
name: laoke-chart-forge
slug: laoke-chart-forge
displayName: 数据可视化图表生成器
description: 零依赖把结构化数据（CSV/JSON）渲染成可直接用的 SVG 柱状图/折线图/饼图。覆盖数据预处理、配色、坐标轴、可读性、嵌入集成的完整方法论，附纯标准库脚本，开箱即用。
version: 1.0.0
author: laoke
category: office-efficiency
license: MIT
compatibility: WorkBuddy / SkillHub / 任意支持 Agent Skills 的客户端；脚本需 Python 3.8+
permissions: 读取用户提供的本地数据文件；在用户指定目录写入 SVG 文件；不联网、不调用外部 API、不读取脚本目录以外的文件
safety: 脚本仅做本地文件读取与 SVG 生成，不执行任何动态代码、不联网、不收集数据。输入文件需为用户自有数据，避免包含未脱敏的个人信息。禁止用于伪造数据或误导性的图表呈现。
---

# 数据可视化图表生成器（Chart Forge）

把杂乱的数据变成一眼看懂的图。本 Skill 提供一套**零依赖、可审计、开箱即用**的图表生产流程：你给它结构化数据（CSV / JSON），它产出标准 SVG 图表（柱状 / 折线 / 饼图），同时交付一整套"怎么把图做对、做清楚、做出专业感"的方法论。

不依赖任何第三方库，脚本只用 Python 标准库，跑完即出文件，不联网、不泄数据。

---

## 一、这个 Skill 解决什么问题

数据中没有人话。老板要看趋势，同事要看占比，客户要看对比——但这些都在 Excel 里躺着，或者散在几段聊天记录里。常见痛点：

1. **想画图但工具门槛高**：装 matplotlib 要配环境，配色要调，中文还乱码。
2. **图做出来了但看不懂**：坐标轴没刻度、图例错位、配色瞎搭，还不如看表格。
3. **图是做好了但没法用**：想嵌进 PPT / 网页 / 文档，导出一堆像素图，放大就糊。
4. **数据没洗就画**：空值、单位不统一、异常值直接上图，结论被带偏。

本 Skill 的打法：**先教你怎么把数据理顺、把图选对、把视觉做干净，再给你一个随时能跑的脚本把活干完。**

---

## 二、什么时候用

| 场景 | 该用哪种图 | 本 Skill 是否合适 |
| --- | --- | --- |
| 看各品类/月份/地区的数值对比 | 柱状图 | ✅ 首选 |
| 看随时间变化的趋势 | 折线图 | ✅ 首选 |
| 看整体构成、占比 | 饼图 | ✅ 仅限少分类 |
| 看相关性、分布 | 散点图 | ⚠️ 本期不含，下版规划 |
| 看多维对比 | 雷达图 | ⚠️ 本期不含 |
| 需要亿级数据实时交互 | 商业 BI | ❌ 不是这个场景 |

**核心判断**：当你要的是"一张能放进汇报、能讲清楚一件事"的静态图，本 Skill 正好。要炫酷大屏、实时联动，那是 BI 工具的活，别硬塞。

---

## 三、快速上手（3 步出图）

### 第 1 步：准备数据文件

CSV（首行表头，UTF-8，可用记事本或 Excel 另存为 CSV）：

```csv
月份,销售额,利润
1月,120,38
2月,150,52
3月,135,41
4月,188,67
5月,210,81
6月,176,59
```

- 单系列：两列即可（`label,value`）。
- 多系列：`label` 在第一列，后面每列一个系列（如上例"销售额""利润"两个系列）。
- 也可以用 JSON（见第六节）。

### 第 2 步：跑脚本

```bash
# 柱状图（默认）
python scripts/chart_forge.py --input 数据.csv --type bar --output 销售图.svg

# 折线图
python scripts/chart_forge.py --input 数据.csv --type line --output 趋势图.svg

# 饼图（只取第一个系列）
python scripts/chart_forge.py --input 数据.csv --type pie --output 占比图.svg

# 不想准备数据？直接看效果
python scripts/chart_forge.py --demo
```

### 第 3 步：用图

生成的 `.svg` 是矢量图：
- **网页**：直接 `<img src="销售图.svg">` 或把 SVG 代码内联进 HTML。
- **PPT / Word**：现代 Office 支持插入 SVG；老版本可转 PNG（见第八节）。
- **文档 / 公众号**：矢量图放大不糊，打印清晰。

---

## 四、三种图表详解

### 4.1 柱状图（bar）

**适用**：分类对比，比如各区域销量、各产品利润、各部门人数。
**参数**：`--type bar`
**多系列行为**：同一分组内并排显示多根柱（分组柱状图），自动留白。

**画好柱状图的 3 个要点**：
1. **基线从 0 开始**。截断 Y 轴会让差异被夸大，是经典的"误导图表"手法，别干。
2. **柱子别太密**。分组超过 5 个系列时，考虑拆成多张图或用折线。
3. **数值标在柱子顶端**（脚本已自动标注），别让人对着刻度线猜。

### 4.2 折线图（line）

**适用**：时间序列趋势，比如日活、营收、温度、排名变化。
**参数**：`--type line`
**多系列行为**：每条线一个颜色，带数据点和图例。

**画好折线图的 3 个要点**：
1. **X 轴必须有序**。月份、日期、版本号才有意义；如果是无序分类，用柱状图。
2. **线条不超过 4 条**。再多就糊成一团，拆图或高亮关键线。
3. **网格线要淡**。脚本用浅灰网格辅助读数，不抢主线。

### 4.3 饼图（pie）

**适用**：整体构成，且分类少（建议 ≤ 6 类）。
**参数**：`--type pie`（只取第一个系列）
**行为**：自动算占比、画扇区、标百分比、出图例。

**画好饼图的铁律**：
1. **只用于"整体=100%"的构成**。销售额占比可以，销售额 vs 利润对比不行（那是对比，用柱状）。
2. **分类别超过 6 个就别用饼**。切成 8 块的饼没人看得清，改用柱状或把长尾合并为"其他"。
3. **占比之和要为 100%**。数据里有负数或异常值会被脚本过滤，先查数据源。
4. **别用 3D 饼图**。立体效果扭曲视觉比例，专业汇报里是减分项。

---

## 五、数据预处理（出图前必做）

图的质量七分靠数据。脚本不替你洗数据，但这几步你必须过一遍：

1. **统一单位**。别一列是"万元"、一列是"元"，画出来对比毫无意义。
2. **处理空值**。空单元格填 0 还是留空？趋势图里留空会断线，先决定策略。
3. **剔除异常值**。明显录入错误（如多敲个 0）会撑爆坐标轴，先标出来再决定去留。
4. **限制分类数**。饼图/柱状图分类超过 10 个就该考虑聚合或换图。
5. **排序**。柱状图按数值降序排，结论更直观；折线图按时间升序。
6. **脱敏**。涉及人名、客户、金额明细的，出图前脱敏，别把原始明细带出去。

---

## 六、数据格式完整规范

### CSV
- 编码 UTF-8（带 BOM 也能识别）。
- 首行表头，列名即系列名/轴名。
- 数值列只能是数字，空值留空（脚本会跳过该行该列）。
- 标签列建议短（如"1月"），太长会挤占 X 轴空间。

### JSON
单系列：
```json
{"type":"pie","title":"渠道占比","points":[["线上",60],["线下",40]]}
```
多系列：
```json
{
  "title":"上半年经营",
  "type":"line",
  "series":[
    {"name":"销售额","points":[["1月",120],["2月",150]]},
    {"name":"利润","points":[["1月",38],["2月",52]]}
  ]
}
```
字段说明：`type` 决定渲染方式；`title` 可选；`series` 是系列数组，每个含 `name` 与 `points`（元素为 `[标签, 数值]`）。

---

## 七、视觉与配色规范

脚本内置 8 色调色板（深蓝 / 金 / 青 / 橙 / 紫 / 灰蓝 / 棕 / 绿），满足绝大多数场景且色觉友好（不含红绿强对比，兼顾色盲可读性）。

**配色原则**：
- **分类少，用强区分色**；系列多了反而要降饱和，避免花哨。
- **不要渐变乱铺**。扁平、清晰比"好看"重要，汇报场景尤其如此。
- **重点高亮**。多系列里想强调某条，可后续手动改其 `fill` 为金色 `#c9a227`。

**字体**：输出 SVG 指定了系统无衬线字体栈（含微软雅黑），中文不乱码；若嵌入不支持中文的环境，预览前确认字体。

**尺寸**：默认 800×520，可通过 `--width` / `--height` 调整。PPT 用图建议宽度 ≥ 960；网页嵌入自适应即可。

---

## 八、输出与集成

### 嵌入网页
```html
<img src="趋势图.svg" alt="销售趋势" style="max-width:100%">
```
或直接把 SVG 全文粘进 HTML（矢量、可交互 hover 提示）。

### 转 PNG（当工具只吃位图时）
脚本产 SVG，转 PNG 用系统工具即可，例如：
- Windows：用浏览器打开 SVG → 截图，或用 Inkscape / ImageMagick `convert 图.svg 图.png`。
- 在线：多数"SVG to PNG"工具可批量转（注意数据不出公网的前提是本地工具）。

### 进 PPT
PowerPoint 2016+ 直接"插入 → 图片 → 选 .svg"。低版本先转 PNG。

---

## 九、常见问题与异常处理

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| 中文乱码 | 文件非 UTF-8 | 另存为 UTF-8（带 BOM 亦可） |
| 报错"不是数字" | 数值列混了文字/空 | 清洗该列，确保纯数字 |
| 饼图只显示一个系列 | 饼图仅取首列系列 | 用单系列 CSV 或 JSON 单 series |
| 柱子/线挤成一团 | 分类或系列过多 | 拆图、聚合长尾、降系列数 |
| 图太大放不下 | 尺寸过高 | 调小 `--height` 或降分类数 |
| 想换配色 | 默认调色板 | 改 `chart_forge.py` 顶部 `PALETTE` 列表 |

**脚本健壮性**：所有用户输入经 XML 转义（`html.escape`），标签/数值里的 `<`、`&` 不会破坏 SVG；非法数据直接报错退出（退出码 2），不产出半成品。

---

## 十、隐私与安全

- 脚本**纯本地运行**，不发起任何网络请求，不上传数据。
- 只读取你显式传入的 `--input` 文件，只在你指定的 `--output` 写文件。
- 不依赖 pip 包，没有供应链风险。
- 数据源建议为脱敏后的聚合数据；原始明细、个人信息不要进图。
- 禁止用途：伪造数据、夸大差异（截断坐标轴）、误导性标注——这些会损害你看图人的信任，也违背职业操守。

---

## 十一、能力边界（说实话）

- ✅ 静态矢量图（柱/折/饼），适合汇报、文档、网页。
- ✅ 零依赖、可审计、可嵌入。
- ❌ 不含散点、雷达、热力、地图、桑基等复杂图（规划中）。
- ❌ 不含实时交互 / 大屏联动（那是 BI 范畴）。
- ❌ 不自动洗数据（数据质量由你把关，见第五节）。

需要更复杂图表时，建议把本 Skill 出的干净数据接入专业 BI 或绘图库，分工明确。

---

## 十一-B、图表选型决策树（照着选不踩坑）

```
你要表达什么？
├─ 比大小 / 比多少？
│   ├─ 分类 ≤ 10 且独立                       → 柱状图
│   └─ 分类 > 10 或需聚合                      → 先聚合再柱状 / 用表格
├─ 看变化 / 看趋势？
│   ├─ 单指标随时间                            → 折线图
│   ├─ 多指标对比趋势                          → 多系列折线图
│   └─ 类别无序（如地区名无时间序）            → 柱状图（别用折线）
├─ 看构成 / 看占比？
│   ├─ 整体=100% 且 分类 ≤ 6                   → 饼图
│   ├─ 分类 > 6 或需看结构变化                 → 柱状图（堆叠）/ 100% 堆叠柱
│   └─ 含负值或"其他"占比过大                  → 柱状图更诚实
└─ 看关系 / 看分布？
    └─ 本期不支持，建议专业 BI / 散点工具
```

记住一句话：**对比用柱、趋势用折、构成用饼（且饼只给少分类）**。选错图型，数据再好也讲不明白。

---

## 十一-C、让图会说话——数据叙事 5 法

图不是终点，让人"秒懂+记住"才是。出图后做这 5 件事：

1. **一句话结论放标题**。别写"销售趋势图"，写"Q2 利润环比增长 21%，主要来自线上渠道"。结论型标题比描述型强 10 倍。
2. **高亮主角**。多系列里把最重要的那条改成金色 `#c9a227`，其余降饱和，视线立刻聚焦。
3. **标注关键拐点**。在某个月份旁加一句"促销上线""政策落地"，让数据有因果。
4. **控制信息量**。一张图只讲一件事。要讲三件事就出三张图，别往一张里硬塞。
5. **给观众指路**。汇报时先说结论，再指图佐证；图是证据不是谜题。

---

## 十一-D、场景模板库（拿来即用）

### 模板 1：周报 / 月报
- 图型：折线（趋势）+ 柱状（本周 vs 上周对比）。
- 数据：本周核心指标、环比、目标完成率。
- 标题范式：`{月份} 核心指标：营收 X 万（环比 +Y%），达标率 Z%`。

### 模板 2：经营财报
- 图型：柱状（各业务线利润）+ 饼图（收入构成）。
- 数据：分业务线营收/成本/利润、占比。
- 注意：金额单位统一为"万元"，饼图分类合并长尾为"其他"。

### 模板 3：运营复盘
- 图型：多系列折线（新增 / 活跃 / 留存）。
- 数据：周期性指标时间序列。
- 标题范式：`{活动} 复盘：活跃峰值 +X%，但 7 日留存仅 Y%，需优化承接`。

### 模板 4：销售看板
- 图型：分组柱状（各区域 / 各销售）。
- 数据：销售额、目标、完成率。
- 注意：Y 轴从 0 起，完成率另起一列或标注。

---

## 十一-E、脚本参数完整参考

| 参数 | 取值 | 默认 | 说明 |
| --- | --- | --- | --- |
| `--input` | 文件路径 | 无 | CSV 或 JSON 数据文件（不填则走 `--demo`） |
| `--type` | `bar`/`line`/`pie` | `bar` | 图表类型 |
| `--output` | 文件路径 | `chart.svg` | 输出 SVG 路径 |
| `--title` | 字符串 | 空 | 图表标题，显示在左上 |
| `--width` | 整数 | 800 | 画布宽（像素） |
| `--height` | 整数 | 520 | 画布高（像素） |
| `--demo` | 开关 | 关 | 生成内置示例数据并出图 |

**退出码**：`0` 成功；`2` 参数或数据错误（详情见 stderr）。

**依赖**：仅 Python 标准库（csv / json / math / sys / os / html），无需 `pip install`，Python 3.8+ 可直接跑。

---

## 十一-F、进阶自定义

1. **换配色**：编辑 `scripts/chart_forge.py` 顶部 `PALETTE` 列表，按顺序替换十六进制色值即可，全局生效。
2. **改默认尺寸**：调整 `WIDTH_DEFAULT` / `HEIGHT_DEFAULT` 常量。
3. **加标题强调色**：在 `render_*` 函数里把标题 `fill` 改为 `#c9a227`（金）。
4. **扩展新图型**：在 `main()` 的 `if/elif` 分支增加新 `render_xxx`，遵循现有签名（`series, W, H, title` → 返回 SVG 字符串）。
5. **批量出图**：写个 shell / bat 循环，对目录下所有 `.csv` 调脚本，一次出一批图。

---

## 十一-G、质量评分卡（交付前自评）

| 维度 | 满分 | 自检问题 |
| --- | --- | --- |
| 选型正确 | 20 | 图型是否匹配要表达的内容？ |
| 数据干净 | 20 | 单位/空值/异常值是否处理？ |
| 视觉清晰 | 20 | 坐标轴/图例/标注是否清楚？ |
| 不失真 | 20 | Y 轴是否从 0 起、无误导？ |
| 可复用 | 20 | 矢量、能嵌入、能脱敏？ |
| **合计** | **100** | ≥80 分才发布/交付 |

低于 80 分的图，回去改，别将就——一张错的图比没有图更伤人。

---

## 十一-H、数据可视化常见 12 个坑（避坑清单）

1. **截断 Y 轴**：不从 0 起，差异被放大。汇报里这是信誉杀手。
2. **双 Y 轴滥用**：两条量纲不同的线硬凑，结论易错。非要用就分开两张图。
3. **3D 饼图**：透视扭曲扇区面积，永远别用。
4. **饼图塞 10 块**：没人分得清，改柱状或合并"其他"。
5. **配色靠彩虹**：红绿蓝黄紫一通堆，色盲友好度为零。用区分度高的有限色板。
6. **无标题或描述型标题**：观众不知道在看啥。结论型标题优先。
7. **网格线抢戏**：粗黑网格盖过数据线。用浅灰（`#e2e8f0`）即可。
8. **数据标签缺失**：让人对着刻度猜。关键数值直接标出来。
9. **单位不标**：图里是"120"还是"120 万"？轴/标题写明单位。
10. **分类无序乱排**：柱状图不排序，对比困难。降序排。
11. **一图塞太多系列**：超过 4 条线就糊。拆图或高亮主线。
12. **用图掩盖坏数据**：数据没洗就画，垃圾进垃圾出。先洗数据（见第五节）。

---

## 十一-I、本 Skill vs Excel / BI 怎么选

| 维度 | 本 Skill（Chart Forge） | Excel 图表 | 商业 BI（如 PowerBI） |
| --- | --- | --- | --- |
| 上手成本 | 极低（一条命令） | 中（手动点） | 高（建模/连接） |
| 依赖 | 零（纯标准库） | 需 Office | 需授权/账号 |
| 输出格式 | 矢量 SVG（可嵌网页/PPT） | 位图/可编辑对象 | 交互仪表盘 |
| 批量/自动化 | ✅ 脚本可循环 | ❌ 手动 | ✅ 但重 |
| 交互/实时 | ❌ 静态 | ❌ | ✅ |
| 适合 | 单张汇报图、嵌入式矢量图、自动化出图 | 随手做图、Excel 内嵌 | 企业级实时看板 |

**一句话**：要一张干净、可嵌入、能自动出的静态图，用本 Skill；要做企业实时大屏，上 BI。各司其职。

---

## 十一-J、端到端实战：从原始数据到汇报图

**背景**：你是运营，手上有上半年各渠道营收（单位：万元），要出一张给老板看的图。

**第 1 步｜拿到原始数据（脏的）**
```
渠道,营收
线上,320
线下,180
代理,,
线下,210
线上,150
```
问题：有空值、单位混、重复行、需要汇总。

**第 2 步｜洗数据（Excel/脚本聚合）**
聚合后得干净汇总：
```csv
渠道,营收
线上,470
线下,390
代理,95
```

**第 3 步｜选型**
要表达"各渠道营收对比" → 柱状图。

**第 4 步｜出图**
```bash
python scripts/chart_forge.py --input 渠道汇总.csv --type bar --output 渠道营收.svg --title "上半年各渠道营收对比（万元）"
```

**第 5 步｜叙事**
标题已是结论型；汇报时口播："线上占 47%，是基本盘，代理渠道虽小但增速快，建议加投。"

**第 6 步｜自检**
Y 轴从 0 起 ✅、分类 ≤ 6 ✅、单位写明 ✅、已脱敏（无客户名）✅。发布。

这 6 步就是标准动作，套到任何数据上都成立。

---

## 十一-K、FAQ 快问快答

**Q：能画横向柱状图吗？**
A：本期纵向柱为主。需要横向时，把数据行列转置后再出图即可（Excel 转置或脚本预处理）。

**Q：中文标签太长被挤怎么办？**
A：缩短标签（如"2026年1月"→"1月"），或在 CSV 里用简称，详情放脚注。

**Q：能导出 PNG 高清图吗？**
A：SVG 是矢量，转 PNG 用浏览器截图或 Inkscape/ImageMagick，分辨率自定义，放多大都不糊。

**Q：数据点很多（几百个）折线会糊吗？**
A：折线按点绘制，点太多会密。建议先降采样/按周期聚合（如按周而非按天），或换柱状。

**Q：能和我的自动化流程串起来吗？**
A：能。脚本退出码规范（0 成功 / 2 失败），可被 cron / 定时任务调用，批量出图后自动汇总。

---

## 十二、使用示例

**示例 1｜月度销售对比（柱状图）**
数据 `sales.csv`：
```csv
月份,销售额,利润
1月,120,38
2月,150,52
3月,188,67
```
命令：
```bash
python scripts/chart_forge.py --input sales.csv --type bar --output sales.svg --title "一季度销售对比"
```
产出：分组柱状图，两根柱（销售额/利润）按月并排，数值标注于顶端。

**示例 2｜半年趋势（折线图）**
```bash
python scripts/chart_forge.py --input sales.csv --type line --output trend.svg --title "销售与利润趋势"
```
产出：双线折线，带数据点、网格、图例。

**示例 3｜渠道占比（饼图）**
`channel.json`：
```json
{"type":"pie","title":"渠道占比","points":[["线上",60],["线下",40]]}
```
```bash
python scripts/chart_forge.py --input channel.json --type pie --output channel.svg
```
产出：饼图，含百分比与图例。

**示例 4｜快速预览**
```bash
python scripts/chart_forge.py --demo
```
产出：内置示例的多系列柱状图 `demo_chart.svg`，用于验证环境。

---

## 十三、自检清单（交付前过一遍）

- [ ] 数据单位统一、空值已处理、异常值已标
- [ ] 图类型匹配问题（对比=柱、趋势=折、构成=饼）
- [ ] 饼图分类 ≤ 6，占比和=100%
- [ ] Y 轴从 0 开始，未截断
- [ ] 中文显示正常，字体栈生效
- [ ] 尺寸适配使用场景（PPT / 网页 / 打印）
- [ ] 已脱敏，无原始个人信息
- [ ] 脚本本地运行，无外发
